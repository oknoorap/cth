/**
 * Build hooks, before and after build process.
 */

/**
 * Before build process.
 */
exports.pre = data => {
  return data
}

/**
 * Map of each row item in data.
 * You can manipulate data per-item here.
 */
exports.each = item => {
  return item
}

/**
 * After build process.
 */
exports.post = data => {
  return data
}
